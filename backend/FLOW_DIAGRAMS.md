# API Flow Diagrams

## 1. Authentication Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ POST /auth/register
     │ {email, password, full_name}
     ▼
┌─────────────────┐
│  Auth Routes    │
└────┬────────────┘
     │
     │ hash_password()
     ▼
┌─────────────────┐
│ User Repository │
└────┬────────────┘
     │
     ├─── SQLite ───┐
     │              │
     └─── MongoDB ──┤
                    │
                    ▼
              ┌──────────┐
              │ Database │
              └──────────┘

Login Flow:
User → POST /auth/login → Verify Password → Generate JWT → Return Token
```

---

## 2. PDF Upload & Module Generation Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ 1. POST /upload/pdf (with PDF file)
     ▼
┌──────────────────┐
│  Upload Routes   │
└────┬─────────────┘
     │
     │ 2. Save to storage/uploads/
     │ 3. extract_text_from_pdf()
     ▼
┌──────────────────┐
│  PDF Parser      │
└────┬─────────────┘
     │
     │ 4. Return extracted_text
     ▼
┌─────────┐
│  User   │ (copies extracted_text)
└────┬────┘
     │
     │ 5. POST /modules/generate-ai
     │    {extracted_text}
     ▼
┌──────────────────┐
│  Module Routes   │
└────┬─────────────┘
     │
     │ 6. generate_module_with_gemini()
     ▼
┌──────────────────┐
│  Gemini API      │
└────┬─────────────┘
     │
     │ 7. Returns {title, content, mcqs}
     ▼
┌──────────────────┐
│  Module Routes   │
└────┬─────────────┘
     │
     │ 8. search_youtube_video(title)
     ▼
┌──────────────────┐
│  YouTube API     │
└────┬─────────────┘
     │
     │ 9. Returns video_id
     ▼
┌──────────────────┐
│ Module Repository│
└────┬─────────────┘
     │
     │ 10. create_module()
     ▼
┌──────────────────┐
│    Database      │
└──────────────────┘
```

---

## 3. MCQ Submission Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ POST /results/submit-mcq
     │ {module_id, answers: [0,1,2,0,1], time_taken}
     ▼
┌──────────────────┐
│  Result Routes   │
└────┬─────────────┘
     │
     │ 1. Get module from repository
     ▼
┌──────────────────┐
│Module Repository │
└────┬─────────────┘
     │
     │ 2. Return module data
     ▼
┌──────────────────┐
│  Result Routes   │
└────┬─────────────┘
     │
     │ 3. calculate_score(user_answers, correct_answers)
     ▼
┌──────────────────┐
│  MCQ Generator   │
└────┬─────────────┘
     │
     │ 4. Return score (e.g., 80.0)
     ▼
┌──────────────────┐
│Result Repository │
└────┬─────────────┘
     │
     │ 5. create_result()
     ▼
┌──────────────────┐
│    Database      │
└────┬─────────────┘
     │
     │ 6. Return result with score
     ▼
┌─────────┐
│  User   │
└─────────┘
```

---

## 4. Chatbot Flow

```
┌─────────┐
│  User   │
└────┬────┘
     │
     │ POST /chatbot/ask
     │ {module_id, question}
     ▼
┌──────────────────┐
│ Chatbot Routes   │
└────┬─────────────┘
     │
     │ 1. Get module from repository
     ▼
┌──────────────────┐
│Module Repository │
└────┬─────────────┘
     │
     │ 2. Return module {title, content, pdf_text}
     ▼
┌──────────────────┐
│ Chatbot Routes   │
└────┬─────────────┘
     │
     │ 3. Build context:
     │    "Module: {title}\nContent: {content}\nPDF: {pdf_text}\nQuestion: {question}"
     ▼
┌──────────────────┐
│  Gemini API      │
└────┬─────────────┘
     │
     │ 4. Generate answer based on context
     ▼
┌──────────────────┐
│ Chatbot Routes   │
└────┬─────────────┘
     │
     │ 5. Return {question, answer}
     ▼
┌─────────┐
│  User   │
└─────────┘
```

---

## 5. Database Switching Flow

```
┌──────────────────┐
│   Any Route      │
└────┬─────────────┘
     │
     │ db = Depends(get_db)
     ▼
┌──────────────────┐
│  database.py     │
│  get_db()        │
└────┬─────────────┘
     │
     │ Check DATABASE_TYPE
     │
     ├─── "sqlite" ───┐
     │                │
     │                ▼
     │         ┌──────────────┐
     │         │  sqlite.py   │
     │         │ AsyncSession │
     │         └──────┬───────┘
     │                │
     │                ▼
     │         ┌──────────────┐
     │         │ SQLAlchemy   │
     │         │   Engine     │
     │         └──────────────┘
     │
     └─── "mongodb" ──┐
                      │
                      ▼
               ┌──────────────┐
               │   mongo.py   │
               │ Motor Client │
               └──────┬───────┘
                      │
                      ▼
               ┌──────────────┐
               │  MongoDB     │
               │  Database    │
               └──────────────┘
```

---

## 6. Repository Pattern Flow

```
┌──────────────────┐
│   Route Layer    │
│ (Database Agnostic)
└────┬─────────────┘
     │
     │ repo = UserRepository(db)
     │ user = await repo.get_user_by_email(email)
     ▼
┌──────────────────────────────────┐
│      User Repository             │
│                                  │
│  async def get_user_by_email():  │
│    if db_type == "sqlite":       │
│      # SQLAlchemy query          │
│    else:                         │
│      # MongoDB query             │
│    return unified_format         │
└────┬─────────────────────────────┘
     │
     ├─── SQLite Path ───┐
     │                   │
     │                   ▼
     │            ┌──────────────┐
     │            │ SELECT * FROM│
     │            │ users WHERE  │
     │            │ email = ?    │
     │            └──────┬───────┘
     │                   │
     │                   ▼
     │            ┌──────────────┐
     │            │ SQLite DB    │
     │            └──────┬───────┘
     │                   │
     │                   │ Return SQLAlchemy object
     │                   ▼
     │            ┌──────────────┐
     │            │ Convert to   │
     │            │ dict format  │
     │            └──────┬───────┘
     │                   │
     └───────────────────┤
                         │
     ┌─── MongoDB Path ──┤
     │                   │
     │                   ▼
     │            ┌──────────────┐
     │            │ db.users     │
     │            │ .find_one()  │
     │            └──────┬───────┘
     │                   │
     │                   ▼
     │            ┌──────────────┐
     │            │ MongoDB      │
     │            └──────┬───────┘
     │                   │
     │                   │ Return document
     │                   ▼
     │            ┌──────────────┐
     │            │ user_helper()│
     │            │ format       │
     │            └──────┬───────┘
     │                   │
     └───────────────────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Unified Dict │
                  │ {id, email,  │
                  │  full_name}  │
                  └──────┬───────┘
                         │
                         ▼
                  ┌──────────────┐
                  │ Route Layer  │
                  │ (Returns to  │
                  │  client)     │
                  └──────────────┘
```

---

## 7. Complete User Journey

```
1. REGISTER
   User → /auth/register → Hash Password → Save to DB → Return User

2. LOGIN
   User → /auth/login → Verify Password → Generate JWT → Return Token

3. UPLOAD PDF
   User → /upload/pdf → Save File → Extract Text → Return Text

4. GENERATE MODULE
   User → /modules/generate-ai → Gemini API → YouTube API → Save Module → Return Module + MCQs

5. TAKE QUIZ
   User → /results/submit-mcq → Calculate Score → Save Result → Return Score

6. VIEW RESULTS
   User → /results/my-results → Fetch from DB → Return All Results

7. GET ANALYTICS
   User → /results/analytics → Calculate Stats → Return Analytics

8. ASK CHATBOT
   User → /chatbot/ask → Fetch Module → Build Context → Gemini API → Return Answer
```

---

## 8. Data Flow

```
┌─────────────────────────────────────────────────────────┐
│                     CLIENT (Browser/Postman)            │
└────────────────────┬────────────────────────────────────┘
                     │
                     │ HTTP Request (JSON)
                     │ + Authorization: Bearer <token>
                     ▼
┌─────────────────────────────────────────────────────────┐
│                     FASTAPI MIDDLEWARE                  │
│                     (CORS, Auth)                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                     ROUTE LAYER                         │
│  - Validate request (Pydantic)                          │
│  - Extract user from JWT                                │
│  - Call repository methods                              │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                  REPOSITORY LAYER                       │
│  - Abstract database operations                         │
│  - Handle SQLite/MongoDB differences                    │
│  - Return unified format                                │
└────────────────────┬────────────────────────────────────┘
                     │
                     ├──────────┬──────────┐
                     ▼          ▼          ▼
              ┌─────────┐ ┌─────────┐ ┌─────────┐
              │ SQLite  │ │ MongoDB │ │Services │
              │   DB    │ │   DB    │ │(AI/PDF) │
              └─────────┘ └─────────┘ └─────────┘
```

---

## 9. Error Handling Flow

```
Request → Route → Try/Catch → Repository → Database
                      │
                      │ If Error
                      ▼
                HTTPException(status_code, detail)
                      │
                      ▼
                FastAPI Error Handler
                      │
                      ▼
                JSON Response {detail: "error message"}
                      │
                      ▼
                Client receives error
```

---

## 10. JWT Authentication Flow

```
1. LOGIN
   User credentials → Verify → Create JWT with {sub: user_id, exp: timestamp}

2. PROTECTED ROUTE
   Request with Authorization header
        │
        ▼
   Extract token from "Bearer <token>"
        │
        ▼
   verify_token(token)
        │
        ├─── Valid ───→ Extract user_id → Continue
        │
        └─── Invalid ─→ HTTPException(401, "Invalid token")
```

---

These diagrams show the complete flow of data through your production-ready backend! 🚀
